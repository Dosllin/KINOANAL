# KINOANAL 🎬🍿
Рекомендательная система фильмов: когда твой ИИ лучше знает, что ты хочешь посмотреть, чем ты сам. Да, снова комедия. Да, снова с Джимом Керри. И да - мы не судим, мы советуем.

- Консольное приложение на Python с несколькими стратегиями рекомендаций: по режиссёрам, по похожим пользователям, по франшизе, по жанру, по топу рейтингов, плюс микс из всех.
- Полный фарш ООП: dataclass’ы для фильмов и пользователей, абстрактная стратегия, инкапсуляция, наследование, полиморфизм, магия.
- Паттерн “Стратегия” в главной роли.

Если кратко: это ваш личный Кинобро. Если не кратко - добро пожаловать.

---


## Почему KINOANAL?
- Потому что “аналитика кино” - это серьёзно, а мы - нет. Шутки, мемы и полезный функционал - у нас всё по подписке “Бесплатно”.
- “Смотри это, тебе зайдёт” - теперь аргументированно и без “ну мне понравилось”.

---

## Фичи, от которых пользователь такой: “🔥”
- **Стратегии**:
  - По режиссёрам (`DirectorStrategy`): находим режиссёров, которых вы чаще всего смотрели, и даём ещё их фильмов.
  - По похожим пользователям (`StrategySimilarUsers`): ищем киновкуса-близнеца и предлагаем то, что вы пропустили.
  - По рейтингу (`RatingStrategy`): топ-10 по среднему рейтингу из базы.
  - Мульти-микс (`MultiStrategy`): собираем рекомендации из всех стратегий и голосуем частотой.
  - По жанру
  - По франшизе
  - Рандом (`Random`): 10 случайных фильмов — отдайся судьбе.
- **Фильтры**:
  - По году (min/max).
  - По рейтингу (min/max для некоторых стратегий).
- **Консольный интерфейс**:
  - Регистрация / логин.
  - Поиск фильма, добавление в просмотренные или в отложенные, проставление оценок.
  - Выбор стратегии рекомендаций и просмотр карточек фильмов с картинками прямо в терминале (через `term-image`).
- **Данные**:
  - Храним в JSON: `Data/films.json`, `Data/user.json`. Добавления/оценки пишутся обратно в файлы.


Технический стэк: Python 100%. Без углеводов, без глютена.

---

## Структура проекта
```text
Controlers/
├─ logining.py         # Вход/регистрация пользователей
├─ manager.py          # Менеджер для добавления просмотров, виш-листа, оценок
└─ searcher.py         # Поиск фильмов по названию, взаимодействие с базой
Data/
├─ films.json          # Фильмотека с метаданными и рейтингами
├─ parsers.py          # Чтение JSON-файлов
└─ user.json           # Пользователи, их жанры и просмотры
GUI/
├─ activation.py       # Меню и запуск алгоритмов, фильтры
└─ display.py          # Отрисовка карточки фильма в консоли (term-image)
Modules/
├─ film.py             # dataclass Film
└─ user.py             # dataclass User
Strategies/
├─ AbstractStrategy.py     # Базовый абстрактный класс
├─ DirectorStrategy.py     # Режиссёры
├─ MultiStrategy.py        # Мульти-микс
├─ Random.py               # Случайные 10
├─ RatingStrategy.py       # По рейтингу
└─ StrategySimilarUsers.py # Похожие пользователи
main.py                    # Точка входа (CLI-меню)
requirements.txt           # Зависимости (term-image + Python >=3.8)
README.md                  # Вы читаете это
```

---

## Примеры использования (консоль)
```bash
git clone https://github.com/Dosllin/KINOANAL.git
cd KINOANAL

# (опционально) venv
python3 -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

pip install -r requirements.txt
python3 main.py
```

---
## Как это работает (коротко по модулям)
- **Данные**: `Parsers` читает `Data/films.json` и `Data/user.json`. Все изменения (просмотры, виш-лист, оценки) записываются обратно.
- **Модели**: `Film`, `User` — dataclass’ы с полями для фильмов/пользователей.
- **Контроллеры**:
  - `logining.py` — вход/регистрация, сбор жанров.
  - `manager.py` — add_viewed/add_wishlist/add_rating.
  - `searcher.py` — поиск фильма, предпросмотр, добавление.
- **Отображение**: `display.py` — печать карточки фильма, обложка через `term-image`, обрезка описания.
- **Стратегии**:
  - `DirectorStrategy` — берёт любимых режиссёров пользователя и рекомендует их фильмы, фильтры по году/рейтингу.
  - `StrategySimilarUsers` — ищет пересечения жанров и просмотров, предлагает фильмы от самых похожих, отдельно «меньше похожие».
  - `RatingStrategy` — сортирует по среднему рейтингу в базе.
  - `MultiStrategy` — объединяет списки стратегий и сортирует по частоте попаданий.
  - `Random` — просто 10 случайных.
- **GUI/activation.py** — меню для активации алгоритмов и установки фильтров.

---

## Пользовательский флоу (CLI)
1. Запуск `python3 main.py` → ASCII-баннер → меню логина/регистрации.
2. Если у вас нет просмотренных — сходите в поиск и добавьте минимум один фильм.
3. Главное меню:
   - `1` — похожие пользователи (с фильтрами).
   - `2` — любимые режиссёры (с фильтрами).
   - `3` — топ по рейтингу (с фильтрами по году).
   - `4` — мульти-микс (все стратегии вместе).
   - `6` — поиск фильма (добавить в просмотренные/отложенные/поставить оценку).
   - `7` — 10 случайных фильмов.
   - `8` — выход.
4. В рекомендациях листайте фильмы, можно сразу добавить в просмотренные или в отложенные.

---

## Лицензия
Пока что “свободная, как ветер”. При желании можно оформить MIT.

---

## От авторов
Этот README - как трейлер: шуток много, но главное - контент. Открывайте main.py, жмите Play и наслаждайтесь рекомендациями. Приятного просмотра! 🎥


<img width="621" height="720" alt="Я запрещаю вам" src="https://github.com/user-attachments/assets/84b66e04-2066-4969-947b-02cddba7ce2f" />
